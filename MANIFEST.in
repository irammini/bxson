# --- MANIFEST.in cho BXSON (Hoàn thiện) ---

# Bao gồm các file metadata quan trọng
include README.md
include LICENSE

# --- Loại trừ Thư mục (Sử dụng 'prune') ---

# Loại trừ các thư mục build/cache/môi trường ảo
prune __pycache__
prune venv
prune dist
prune build
prune .github

# Loại trừ thư mục egg-info đã được tạo
prune bxson.egg-info 

# --- Loại trừ Tệp (Sử dụng 'global-exclude' và 'exclude') ---

# Các file tạm thời/cache (ở bất kỳ đâu)
global-exclude *.pyc
global-exclude *.pyo
global-exclude *.egg-info  # Giữ lại để bắt các trường hợp khác

# Loại trừ file cấu hình hệ thống ở thư mục gốc
exclude .gitignore
exclude setup.cfg